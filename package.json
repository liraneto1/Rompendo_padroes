{
  "name": "rompa-teste-padroes",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "start:production": "node scripts/start-production.mjs",
    "build": "node --check scripts/serve.mjs && node --check server/api.mjs && node --check server/runtime.mjs && node --check server/store.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=24" }
}
